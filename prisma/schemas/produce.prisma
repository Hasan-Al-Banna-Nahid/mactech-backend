model Produce {
  id                  String        @id @default(uuid())
  vendorId            String
  vendor              VendorProfile @relation(fields: [vendorId], references: [id])
  name                String
  description         String
  price               Float
  category            String
  certificationStatus Status        @default(PENDING)
  availableQuantity   Int
  orders              Order[]
}
